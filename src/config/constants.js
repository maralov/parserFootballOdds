'use strict';

/** Base URL of the flashscore mobile site. */
const FLASHSCORE_MOBI_BASE = 'https://www.flashscore.mobi';

/** Default live board URL (football section). */
const FLASHSCORE_LIVE_URL = `${FLASHSCORE_MOBI_BASE}/?s=2`;

module.exports = { FLASHSCORE_MOBI_BASE, FLASHSCORE_LIVE_URL };
